<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--
        Vinc Guide — Admin track (4 sub-pages).
        Owns: how an admin rolls Vinc out across a team and wires it into existing systems.
        Each page t-calls qr_code_odoo.vinculum_guide_layout.
    -->

    <!-- ========================= BULK ONBOARDING ========================= -->
    <template id="vinculum_guide_bulk_onboard" name="Bulk Onboarding">
        <t t-call="qr_code_odoo.vinculum_guide_layout">
            <div class="vg-breadcrumb">
                <a href="/vinculum/guide">Guide</a>
                <span class="vg-bc-sep">/</span>
                <span>For Administrators</span>
                <span class="vg-bc-sep">/</span>
                <span class="vg-bc-current">Bulk Onboarding</span>
            </div>
            <div class="vg-page-header">
                <h1 class="vg-display">Seed cards for your <em>existing team</em> in one upload.</h1>
                <p class="vg-summary">CSV in, cards live. Each row maps to an Odoo user that already exists; the upload creates the vCards and emails each person a "your card is ready" link.</p>
            </div>
            <div class="vg-content">

                <div class="vg-callout" style="background: #EEF2FF; border-left: 3px solid #2D5BFF; padding: 14px 18px; border-radius: 8px; margin-bottom: 28px;">
                    <strong>Who can run this:</strong> the <code>/bulk-onboard</code> flow is locked to users with the <em><t t-out="brand_name"/> Manager</em> role (or Odoo system admins, who inherit it). Grant it from <em>Settings → Users &amp; Companies → Users → Access Rights → <t t-out="brand_name"/></em>. Regular internal users have no access to the bulk-onboarding models via RPC or UI.
                </div>

                <div class="vg-callout" style="background: #FFF7ED; border-left: 3px solid #EA580C; padding: 14px 18px; border-radius: 8px; margin-bottom: 28px;">
                    <strong>Read first:</strong> bulk onboarding now requires every email in your CSV to <em>already exist</em> as an Odoo user (under <em>Settings → Users &amp; Companies → Users</em>). The upload no longer creates user accounts, no longer sends magic-link activation emails, and no longer asks the recipient to set a password. <t t-out="brand_name"/> is an app on top of Odoo — your reps come from Odoo's standard user-management flow, and this wizard just gives them a vCard.
                </div>

                <h2><span class="vg-h2-num">01</span>The CSV format</h2>
                <p>Bulk onboarding takes a CSV with one row per future card. Two columns are required, the rest are optional:</p>
                <ul>
                    <li><strong>Required:</strong> <code>name</code>, <code>email</code> (must match an existing Odoo user's login)</li>
                    <li><strong>Profile:</strong> <code>phone</code>, <code>mobile</code>, <code>function</code> (job title), <code>about</code></li>
                    <li><strong>Address:</strong> <code>street</code>, <code>street2</code>, <code>city</code>, <code>zip</code>, <code>country</code>, <code>state</code></li>
                    <li><strong>Social:</strong> <code>linkedin_url</code>, <code>facebook_url</code>, <code>twitter_url</code>, <code>instagram_url</code>, <code>whatsapp_url</code>, <code>youtube_url</code>, <code>calendly_url</code></li>
                    <li><strong>URL:</strong> <code>website_slug</code> (auto-generated from name if omitted)</li>
                </ul>
                <p>Unknown columns are ignored, so it's safe to upload a richer HR export and let <t t-out="brand_name"/> pick what it needs.</p>
                <p><a href="/bulk-onboard/template">📥 Download the starter CSV</a> — pre-populated with five example rows, headers, and per-column hints in the first row.</p>

                <h2><span class="vg-h2-num">02</span>The upload flow</h2>
                <p>From the user dashboard, click <em class="vg-mark">Bulk Onboarding</em> (admin-only button). You'll land on the wizard at <code>/bulk-onboard</code>:</p>
                <ol>
                    <li><strong>Upload CSV</strong> — drag-and-drop or pick a file. <t t-out="brand_name"/> parses and previews the first 5 rows. The upload response also reports how many emails matched existing Odoo users — a yellow banner appears listing any rows that will be skipped because no matching user exists.</li>
                    <li><strong>Review &amp; map</strong> — confirm column mappings; fix typos in row data inline. If you see unmatched-email rows in the banner, either remove them from the CSV, or first create those users in <em>Settings → Users &amp; Companies → Users</em> and re-upload.</li>
                    <li><strong>Set defaults</strong> — pick a template, primary/secondary colors, and lead-form behavior to apply to every card. Per-row overrides win.</li>
                    <li><strong>Submit</strong> — <t t-out="brand_name"/> creates the vCards (one per matched row) in a single transaction and emails each user a "your card is ready" notification.</li>
                </ol>
                <div class="vg-note">
                    The wizard is <strong>lenient</strong> about unmatched rows: matched rows still process, unmatched rows land in the batch's error log so you can tell exactly who got skipped and why. Nothing about the existing flow is half-committed.
                </div>

                <h2><span class="vg-h2-num">03</span>Notification emails</h2>
                <p>Each user gets a short email when their card is created: subject "Your <t t-out="brand_name"/> card is ready at &lt;company&gt;", body with a single <em class="vg-mark">View your card →</em> button that deep-links to the new vCard form. They sign in with the Odoo password they already use; if they're already logged in the link drops them straight onto the card.</p>
                <p>The template is a regular Odoo <code>mail.template</code> (look for <code>Vinc — Bulk Onboarding Invitation</code> in <em>Settings → Technical → Email Templates</em>). See <a href="/vinculum/guide/automations">Email Automations &amp; Digests</a> for the full template-editing workflow.</p>

                <h2><span class="vg-h2-num">04</span>Troubleshooting common rows</h2>
                <p>Per-row issues bubble up in the batch's error log after submit, the wizard keeps going, and unmatched rows don't half-commit. Common reasons a row fails:</p>
                <ul>
                    <li><strong>No Odoo user found for &lt;email&gt;</strong> — the email doesn't match any <code>res.users.login</code>. Add the user under <em>Settings → Users &amp; Companies → Users</em> first, then re-upload.</li>
                    <li><strong>Duplicate slug</strong> — two rows resolved to the same <code>website_slug</code>. Fix one or supply explicit slugs.</li>
                    <li><strong>Duplicate email within the upload</strong> — <t t-out="brand_name"/> dedupes within the file before insert.</li>
                    <li><strong>Slug collision with an existing card</strong> — pre-existing card has the same slug. Reassign.</li>
                    <li><strong>Invalid email</strong> — failed format validation. Fix or remove.</li>
                </ul>
                <div class="vg-note">
                    Bulk uploads can legitimately seed a <strong>second</strong> card for a user who already has one (different role, different audience, different show). If you'd rather not, leave those rows out of the CSV.
                </div>
                <div class="vg-note">
                    There's no hard row limit, but in practice keep batches under ~500 rows so the notification-email queue doesn't backlog. For larger rollouts, split into multiple uploads and stagger them.
                </div>

            </div>
        </t>
    </template>


    <!-- ============================== CRM ============================== -->
    <template id="vinculum_guide_crm" name="CRM &amp; Lead Routing">
        <t t-call="qr_code_odoo.vinculum_guide_layout">
            <div class="vg-breadcrumb">
                <a href="/vinculum/guide">Guide</a>
                <span class="vg-bc-sep">/</span>
                <span>For Administrators</span>
                <span class="vg-bc-sep">/</span>
                <span class="vg-bc-current">CRM &amp; Lead Routing</span>
            </div>
            <div class="vg-page-header">
                <h1 class="vg-display">From card scan to <em>pipeline-ready</em> opportunity.</h1>
                <p class="vg-summary">How <t t-out="brand_name"/>-captured leads land in Odoo CRM, who picks them up, and where you tune the rules.</p>
            </div>
            <div class="vg-content">

                <h2><span class="vg-h2-num">01</span>How vCard leads map to <code>crm.lead</code></h2>
                <p>When a visitor submits the lead form on a card, <t t-out="brand_name"/> creates one <code>crm.lead</code> record (Odoo CRM's opportunity model). The mapping:</p>
                <ul>
                    <li><strong><code>name</code></strong> — derived from the cardholder + visitor (e.g., "Lead from Jane's card — John Smith")</li>
                    <li><strong><code>contact_name</code>, <code>email_from</code>, <code>phone</code></strong> — straight from the form</li>
                    <li><strong><code>partner_vcard_id</code></strong> — <t t-out="brand_name"/>-specific: links back to the card that captured the lead</li>
                    <li><strong><code>tag_ids</code></strong> — the lead tags configured on the card at capture time</li>
                    <li><strong><code>type</code></strong> = <code>opportunity</code></li>
                    <li><strong><code>description</code></strong> — the visitor's notes, if your form's notes field is enabled</li>
                </ul>

                <h2><span class="vg-h2-num">02</span>Lead routing rules</h2>
                <p><t t-out="brand_name"/> auto-assigns each new opportunity to the card's owner. The resolution order is:</p>
                <ol>
                    <li>Look up a <code>res.users</code> whose login matches the card's <code>email</code>. If found, that user becomes <code>user_id</code>.</li>
                    <li>Otherwise fall back to the user who created the card record (<code>partner.create_uid</code>), unless that's a system / public user (id ≤ 4).</li>
                    <li>If neither resolves, the opportunity is left unassigned and Odoo's standard team rules take over.</li>
                </ol>
                <p>For more sophisticated patterns — round-robin across a sales team, geo-based routing, account-based routing — layer Odoo's standard CRM lead-assignment rules on top. They run after <t t-out="brand_name"/>'s <code>user_id</code> assignment, so configure them to either trust <t t-out="brand_name"/>'s pick or override it explicitly.</p>
                <div class="vg-note">
                    To opt out of <t t-out="brand_name"/>'s auto-assign and rely entirely on Odoo's team-based rules, leave the cardholder's email distinct from any Odoo user login. The fallback to <code>create_uid</code> is skipped for system users, so cards created via the public <code>/get-started</code> route by anonymous visitors don't accidentally get assigned to OdooBot.
                </div>

                <h2><span class="vg-h2-num">03</span>Pipeline integration</h2>
                <p><t t-out="brand_name"/>-captured leads behave like any other CRM opportunity. They show up in:</p>
                <ul>
                    <li>The standard pipeline kanban (filterable by <t t-out="brand_name"/> tag)</li>
                    <li>List, calendar, and graph views</li>
                    <li>The user dashboard's "Recent Leads" widget (<t t-out="brand_name"/>-only filter)</li>
                </ul>
                <p>The "<t t-out="brand_name"/>-only filter" is a saved search on <code>partner_vcard_id ≠ False</code> — useful for measuring how <t t-out="brand_name"/> compares to your other lead sources.</p>

                <h2><span class="vg-h2-num">04</span>Lead tags as your reporting backbone</h2>
                <p>Lead tags applied at capture time are the single most useful piece of structured data for downstream reporting. Treat them as the spec for your weekly review:</p>
                <ul>
                    <li><strong>Source attribution</strong> — which card / channel / event drove this lead</li>
                    <li><strong>Intent qualification</strong> — what the visitor was after</li>
                    <li><strong>Follow-up triage</strong> — who picks this up next</li>
                </ul>
                <p>Educate cardholders on tags during onboarding. Tags applied 30 days later, when context is gone, are noise.</p>

                <h2><span class="vg-h2-num">05</span>Look-alike matches</h2>
                <p>Every lead captured through a card automatically gets scored against records you already have. The point isn't to find duplicates; it's to give the rep a memory aid. When a fresh prospect arrives, the lead form shows up to three companies and three open opportunities that <em>resemble</em> them, each annotated with the specific signals that matched <em>and</em> the conversation context (won/lost/active, deal size, last note, owner).</p>

                <h3>The signals (similarity, not equality)</h3>
                <p>Five signals, weighted to sum to 100:</p>
                <ul>
                    <li><strong>Industry hierarchy</strong> (weight: 35) — exact match on <code>industry_id</code> gets the full 35; sibling-industry match (same parent in the <code>res.partner.industry</code> tree) gets 17. So "SaaS" and "IT Services" still match because both sit under "Software".</li>
                    <li><strong>Geographic gradient</strong> (weight: 25) — same city = full 25; same state = 15; same country = 8. Closer geography weighs more, no all-or-nothing penalty.</li>
                    <li><strong>Deal-size bucket</strong> (weight: 20) — <code>expected_revenue</code> bucketed into <em>micro</em> (&lt;$10k), <em>small</em> ($10–50k), <em>mid</em> ($50–200k), <em>large</em> ($200k+). Same bucket = full 20; adjacent bucket = 10. For company candidates, the bucket is derived from their highest historical <code>expected_revenue</code> in CRM.</li>
                    <li><strong>Tag word-overlap</strong> (weight: 15) — fuzzy on tag NAMES split into words. "FinTech B2B" matches "Financial Services" because both contain a domain word. Cross-model (lead vs partner) so a <code>crm.tag</code> can match a <code>res.partner.category</code>.</li>
                    <li><strong>Description Jaccard</strong> (weight: 5, capped) — token overlap between the lead's <code>description</code> and the candidate's notes. Small weight; mostly a tiebreaker / serendipity signal.</li>
                </ul>
                <p>A candidate needs <strong>≥ 25 points</strong> to surface. Top 3 per bucket are shown.</p>

                <h3>The conversation-starter card</h3>
                <p>Each surfaced match gets a small card on the lead form with the actual context the rep needs to talk:</p>
                <ul>
                    <li><strong>Subtitle</strong> — industry, city or country (whatever's populated)</li>
                    <li><strong>Outcome</strong> — for companies: their best-deal label (Won / Lost / Active), amount, close date, owner. Lost reason if present. For open leads: stage, amount, owner.</li>
                    <li><strong>Snippet</strong> — first 120 chars of the partner's <code>comment</code> or the lead's <code>description</code>. The fastest "what was this about" cue.</li>
                </ul>
                <p>Example card:</p>
                <pre style="background: #f8f9fa; padding: 12px; border-radius: 6px; font-size: 0.9em;"><code>Acme Corp — SaaS, San Francisco
same industry (SaaS), same city (San Francisco), same deal-size band (mid)
Won deal · $87,000 · closed Mar 2026 · Sarah Chen
"Migrated from Salesforce, valued our API rate limits"</code></pre>

                <h3>Pool definitions</h3>
                <ul>
                    <li><strong>Companies pool</strong>: active companies in <code>res.partner</code>, excluding pure suppliers (<code>supplier_rank &gt; 0</code> with <code>customer_rank == 0</code>). The lead's own partner is excluded. Capped at 2,000.</li>
                    <li><strong>Open-leads pool</strong>: opportunities (<code>type='opportunity'</code>) not yet won (<code>probability &lt; 100</code>) and not lost (<code>probability &gt; 0</code>). The current lead is excluded once saved. Capped at 2,000.</li>
                </ul>

                <h3>Quality scales with your CRM hygiene</h3>
                <p>The feature is only as good as the data on your existing records. Highest-leverage things to populate:</p>
                <ul>
                    <li><strong>Industry</strong> on customer companies — the heaviest signal. Set <code>industry_id</code> on the partner record.</li>
                    <li><strong>City</strong> on customer addresses — much sharper than country alone.</li>
                    <li><strong>Tags</strong> with meaningful words (not "important" or "to-follow"). The fuzzy matcher splits on words, so "B2B SaaS" and "FinTech B2B" share the "b2b" signal.</li>
                    <li><strong>Notes</strong> in <code>comment</code> (partner) or <code>description</code> (lead). Even one sentence per record makes the snippet card useful.</li>
                </ul>
                <p>A new tenant on day one with sparse customer records sees a hint on every lead explaining what's missing.</p>

                <h3>What it doesn't do</h3>
                <ul>
                    <li><strong>No enrichment.</strong> Works on the data already in your database. Doesn't call Apollo, Clearbit, ZoomInfo or anyone else. Privacy posture is unchanged.</li>
                    <li><strong>No historical reprocessing.</strong> Matches are computed live each time a lead is opened. Adding industry to a customer today benefits every lead opened tomorrow.</li>
                    <li><strong>No write-back.</strong> Read-only suggestion surface.</li>
                    <li><strong>No multi-currency normalization.</strong> Deal-size buckets use the lead's expected_revenue value as-is. Mixed-currency tenants will see noisier matches until we add per-company normalization in a future pass.</li>
                </ul>

                <h2><span class="vg-h2-num">06</span>Event binding (trade shows, conferences, open houses)</h2>
                <p>Cards have an <em>Active Event</em> field (in the Lead Collection Form Settings group on the card form). When a rep is staffing a booth, they pick the event there for the day. Every lead captured by that card while the field is set gets stamped with <code>crm.lead.event_id = &lt;the event&gt;</code>, which means:</p>
                <ul>
                    <li>A small <strong>Captured at: &lt;event&gt;</strong> badge appears under the lead's title in CRM, so anyone opening the lead immediately sees the source.</li>
                    <li>Standard Odoo CRM filters work out of the box — saved searches like "leads from SaaStr 2026", "ROI per event", "conversion rate by show". The <code>event_crm</code> module already provides the field; <t t-out="brand_name"/> just hooks captures into it.</li>
                </ul>

                <h3>What it doesn't do</h3>
                <ul>
                    <li><strong>No event registration is created.</strong> An earlier version did this so the prospect appeared on the event's official attendee list, but every <code>event.registration</code> triggers Odoo's "thanks for registering" email plus any pre-scheduled day-of reminders — meaning a single scan spammed the prospect with up to 10 emails about an event they never knowingly signed up for. The lead-side <code>event_id</code> is enough for the reporting; if a rep wants the prospect on the official attendee list, it's two clicks to add them manually from CRM.</li>
                    <li><strong>One active event per card.</strong> The rep has to manually clear the field after the show. We may add a daily cron later that nulls it the day after <code>event.date_end</code> — for now it's manual.</li>
                    <li><strong>No booth-level tagging.</strong> Odoo's <code>event_booth</code> module supports "which booth at the show" — easy to layer on if anyone asks.</li>
                </ul>

                <div class="vg-callout" style="background: #EEF2FF; border-left: 3px solid #2D5BFF; padding: 14px 18px; border-radius: 8px; margin-top: 16px;">
                    <strong>Dependency:</strong> the <code>event_crm</code> module (Odoo Community, free) is a hard dependency of <t t-out="brand_name"/>. On install or upgrade, Odoo pulls it in automatically — no manual setup.
                </div>

            </div>
        </t>
    </template>


    <!-- ============================== NFC ============================== -->
    <template id="vinculum_guide_nfc" name="NFC Card Programming">
        <t t-call="qr_code_odoo.vinculum_guide_layout">
            <div class="vg-breadcrumb">
                <a href="/vinculum/guide">Guide</a>
                <span class="vg-bc-sep">/</span>
                <span>For Administrators</span>
                <span class="vg-bc-sep">/</span>
                <span class="vg-bc-current">NFC Card Programming</span>
            </div>
            <div class="vg-page-header">
                <h1 class="vg-display">Programming <em>NFC cards</em> for tap-to-share.</h1>
                <p class="vg-summary">A 10-minute walkthrough that takes a blank NFC card from box to working in someone's hand.</p>
            </div>
            <div class="vg-content">

                <h2><span class="vg-h2-num">01</span>Recommended cards</h2>
                <p>Buy NTAG215-chip cards. They're widely available, hold enough memory for a URL many times over, and work with every modern phone (iOS 13+, Android 4.4+). Skip the cheaper NTAG213 — they're tighter on memory and you'll regret it the first time you want to encode anything richer.</p>
                <p>Search "NTAG215 PVC card" on your usual supplier (Amazon, AliExpress, or a regional NFC distributor). Look for matte-finish PVC cards if you want to print on them, or pre-printed blanks if you'll add a sticker. Avoid the keychain fobs — they're awkward to hand to people.</p>

                <h2><span class="vg-h2-num">02</span>Programming via NFC Tools</h2>
                <p>Install <strong>NFC Tools</strong> on your phone (iOS App Store or Google Play, free). The flow is the same on both platforms:</p>
                <ol>
                    <li>Open NFC Tools, tap <em class="vg-mark">Write</em>, then <em class="vg-mark">Add a record</em>.</li>
                    <li>Pick <em class="vg-mark">URL / URI</em>. Paste your card's URL (e.g., <code>yoursite.com/jane-doe</code>).</li>
                    <li>Tap <em class="vg-mark">OK</em>, then <em class="vg-mark">Write</em>. Hold the card flat against the back of your phone until you hear the confirmation beep.</li>
                </ol>
                <p>Two cards in two minutes once you've done the first. For a team rollout, bring the box of blanks to a single rollout meeting and program them assembly-line.</p>

                <h2><span class="vg-h2-num">03</span>Direct-write vs URL-write</h2>
                <p>NFC Tools offers two modes; URL-write is what you want.</p>
                <ul>
                    <li><strong>URL-write (recommended):</strong> the card stores your <t t-out="brand_name"/> URL. When tapped, the phone opens the URL. Updates are instant — change the page server-side, every card already in the wild reflects the change on the next tap.</li>
                    <li><strong>Direct-write (avoid):</strong> the card stores the contact data directly as a vCard payload. The phone shows "Save contact?" without opening a page. Sounds smooth, but: no analytics, no lead form, no updates after the card is printed. You're stuck.</li>
                </ul>
                <div class="vg-note">
                    The whole point of <t t-out="brand_name"/> is the live page behind the URL. Direct-write throws that away.
                </div>

                <h2><span class="vg-h2-num">04</span>Testing</h2>
                <p>Before handing the card to a customer, tap-test it on:</p>
                <ul>
                    <li>Your own phone (sanity check)</li>
                    <li>A different OS (e.g., test on iOS if you wrote with Android, and vice versa)</li>
                    <li>A locked phone (NFC works on iOS even when locked, on most Androids too)</li>
                </ul>
                <p>If the tap doesn't trigger, 90% of the time the card is just oriented wrong — try moving it slowly across the back of the phone until the chip aligns with the phone's NFC antenna (varies by model).</p>

            </div>
        </t>
    </template>


    <!-- =========================== AUTOMATIONS =========================== -->
    <template id="vinculum_guide_automations" name="Email Automations &amp; Digests">
        <t t-call="qr_code_odoo.vinculum_guide_layout">
            <div class="vg-breadcrumb">
                <a href="/vinculum/guide">Guide</a>
                <span class="vg-bc-sep">/</span>
                <span>For Administrators</span>
                <span class="vg-bc-sep">/</span>
                <span class="vg-bc-current">Email Automations &amp; Digests</span>
            </div>
            <div class="vg-page-header">
                <h1 class="vg-display">The <em>automation stack</em>, end to end.</h1>
                <p class="vg-summary">Three flavors of automated follow-up — intro email, click-to-chat, and digest. Each has a job; each is editable.</p>
            </div>
            <div class="vg-content">

                <h2><span class="vg-h2-num">01</span>Intro emails</h2>
                <p>When a lead is captured on a card with intro emails enabled, <t t-out="brand_name"/> fires a one-shot intro email <strong>synchronously, immediately</strong> after the opportunity is created. No cron, no delay. The template uses Odoo's standard mail-template system, with placeholders for the lead's name, the cardholder's name, phone, email, and the card URL.</p>
                <p>Intro emails are <em class="vg-mark">opt-in per card</em> (toggle <em>Send intro email automatically</em> on the card's form view, in the Lead Collection Form Settings group). When off, leads still land in CRM but no automated reply goes out.</p>
                <div class="vg-note">
                    Because intro emails fire inline with the lead-capture request, your outgoing mail server's responsiveness affects the lead form's response time. If your SMTP host is slow, lead submissions feel slow. Use a transactional sender (Postmark, Mailgun, AWS SES) for sub-second send times.
                </div>
                <p>Intro Email is the only automated email path. An earlier version of the addon also had a separate "Leadback automated email" toggle that did essentially the same job with a configurable delay; that toggle was removed because cards with both flags on double-mailed every prospect. If you need a delayed (rather than immediate) follow-up, use Odoo's standard <em>Mail → Marketing</em> automation against the captured lead.</p>

                <h2><span class="vg-h2-num">02</span>Click to Chat</h2>
                <p>Click to Chat (formerly "Instant Lead-Back") shows the lead a panel of tap-to-message buttons immediately after they submit the form. The buttons open the lead's native messaging app pre-filled with a templated message.</p>
                <ul>
                    <li><strong>Toggle</strong>: <em>Click to Chat</em> in the Click to Chat Settings group on the card form. When on, the rest of the section unlocks.</li>
                    <li><strong>Channels</strong>: pick any combination of <strong>WhatsApp</strong>, <strong>Viber</strong>, <strong>Telegram</strong>. WhatsApp is auto-selected when you flip the master toggle on (so you don't trip the "pick a channel" validator immediately).</li>
                    <li><strong>Message Delay</strong>: how long to wait before the messaging links appear (immediate / 1 min / 5 min / 15 min / 1 hour / custom).</li>
                    <li><strong>Chat Message Template</strong>: a plain-text body with the same placeholder syntax used in intro emails — <code>{contact_name}</code>, <code>{vcard_url}</code>, <code>{booking_url}</code>, conditional <code>{if field}…{/if}</code> blocks, and fallback <code>{placeholder|default}</code>.</li>
                </ul>
                <p>A common pattern: intro email goes out immediately (the formal touch), and a 5-minute Click to Chat panel surfaces (the informal "let's talk now" touch). Both are opt-in per card.</p>

                <h2><span class="vg-h2-num">03</span>Weekly digest emails</h2>
                <p>Each cardholder can receive a weekly digest summarizing their card's performance: scans, views, leads captured, top-rated reviews. The digest is opt-in and configurable per user.</p>
                <p>For administrators, the org-level digest aggregates across all cards your account manages, with leaderboards by lead count and review count. Useful for sales-team standups.</p>

                <h2><span class="vg-h2-num">04</span>Editing templates</h2>
                <p>All the addon's mail templates live in Odoo's standard <em class="vg-mark">Settings → Technical → Email Templates</em>, prefixed with <code>Vinc —</code> so they cluster alphabetically. Search for <code>Vinc —</code> in the list and you'll see:</p>
                <ul>
                    <li><strong>Vinc — Introduction Email (to new lead)</strong> — fires when a lead is captured and <em>intro email</em> is enabled on the card.</li>
                    <li><strong>Vinc — New Lead Notification (to card owner)</strong> — alerts the cardholder when someone submits their form.</li>
                    <li><strong>Vinc — Digest Email</strong> — the weekly/monthly performance summary.</li>
                    <li><strong>Vinc — Bulk Onboarding Invitation</strong> — activation email for bulk-onboarded users.</li>
                    <li><strong>Vinc — Bulk Onboarding Completion</strong> — admin summary after a bulk batch completes.</li>
                </ul>
                <p>They're regular Jinja-style templates with full access to the lead, card, and user records. Customize tone, add brand fields, swap CTAs.</p>
                <div class="vg-warn">
                    Editing email templates is permanent across all cards using that template. If you want a per-card variant, duplicate the template first and assign the duplicate on the relevant card's form view.
                </div>
                <figure class="vg-figure">
                    <img src="/qr_code_odoo/static/src/img/guide/email-templates-vinc.png"
                         alt="The Settings → Technical → Email Templates list filtered to the addon's prefix, showing all 5 templates"
                         loading="lazy"/>
                    <figcaption><strong>Fig. 4</strong>Settings → Technical → Email Templates, filtered to <code>Vinc</code>.</figcaption>
                </figure>

            </div>
        </t>
    </template>


    <!-- ============================ BRANDING ============================ -->
    <template id="vinculum_guide_branding" name="Branding &amp; White-Label">
        <t t-call="qr_code_odoo.vinculum_guide_layout">
            <div class="vg-breadcrumb">
                <a href="/vinculum/guide">Guide</a>
                <span class="vg-bc-sep">/</span>
                <span>For Administrators</span>
                <span class="vg-bc-sep">/</span>
                <span class="vg-bc-current">Branding &amp; White-Label</span>
            </div>
            <div class="vg-page-header">
                <h1 class="vg-display">Make it look like <em>your</em> product.</h1>
                <p class="vg-summary">Set a brand name and logo once; it propagates to every user-facing surface — emails, dashboards, public vCard pages, the Apps grid tile.</p>
            </div>
            <div class="vg-content">

                <h2><span class="vg-h2-num">01</span>Where to set it</h2>
                <p>Open <em class="vg-mark">Settings</em> from the main app menu. Scroll the left rail until you find the <strong><t t-out="brand_name"/></strong> section — there's one block, "Branding", with four fields:</p>
                <ul>
                    <li><strong>Brand Name</strong> — full product name, used in prose. Default: "Vinc".</li>
                    <li><strong>Short Name</strong> — compact version for tight spaces (logo chips, wordmarks). Default: "Vinc".</li>
                    <li><strong>Brand Icon</strong> — square logo (~512×512 PNG). Used for chips, favicons, the Apps grid tile, and the default QR-code center overlay.</li>
                    <li><strong>Brand Wordmark</strong> — horizontal logo for page headers and email banners. Falls back to the icon if blank.</li>
                </ul>
                <p>Save the form. Hard-refresh the browser (Ctrl+Shift+R / Cmd+Shift+R) and the new branding shows up everywhere immediately.</p>

                <h2><span class="vg-h2-num">02</span>What rebrands</h2>
                <ul>
                    <li><strong>The Apps grid tile</strong> — the icon and label on the main Apps menu use your uploaded values.</li>
                    <li><strong>Top-level menu</strong> — the app's root menu name and icon (the one between the chatter and the screen).</li>
                    <li><strong>Public vCard pages</strong> — magic-link onboarding, NFC setup wizard, vCard form headers, the Vinculum Guide top bar — all read the brand name at render time.</li>
                    <li><strong>Email subjects and bodies</strong> — bulk-onboarding "your card is ready" email, weekly digest footer. Each email reads the brand from <code>ir.config_parameter</code> at send time, so a brand change applied today retroactively rebrands tomorrow's emails without resending.</li>
                    <li><strong>QR code center overlay</strong> — when a card hasn't uploaded its own QR logo, the overlay defaults to your Brand Icon.</li>
                </ul>

                <h2><span class="vg-h2-num">03</span>What stays "Vinc"</h2>
                <p>A few surfaces are deliberately not rebrandable, by design or by Odoo constraint:</p>
                <ul>
                    <li><strong>The technical module name</strong> (<code>qr_code_odoo</code>) — internal identifier, never user-visible.</li>
                    <li><strong>Email template <em>names</em></strong> in <em>Settings → Technical → Email Templates</em> — they still show as <code>Vinc — Digest Email</code> etc. because admins use those names to find templates. The bodies rebrand; the labels don't.</li>
                    <li><strong>App-store listing</strong> (the <code>static/description/index.html</code> rendered on apps.odoo.com) — that's the marketing page for the public Vinc product, not a per-tenant surface.</li>
                </ul>

                <h2><span class="vg-h2-num">04</span>Persistence across upgrades</h2>
                <p>Odoo's standard behavior on module upgrade is to re-import data files, which would normally reset the menu's name and icon back to the bundled defaults. <t t-out="brand_name"/> hooks into Odoo's registry-load step to <em>re-apply</em> your saved brand on every server start, so a future <code>-u qr_code_odoo</code> doesn't undo your customization. The brand-name and short-name strings live in <code>ir.config_parameter</code>; the icon and wordmark binaries live in <code>ir.attachment</code>.</p>
                <div class="vg-callout" style="background: #EEF2FF; border-left: 3px solid #2D5BFF; padding: 14px 18px; border-radius: 8px; margin-top: 16px;">
                    <strong>Smoke test after upgrades:</strong> hard-refresh the Apps grid. If the tile reverts to the default icon, do <em>Settings → Vinc → Save</em> once and refresh again — that re-fires the sync method that talks to the menu/module records.
                </div>

            </div>
        </t>
    </template>

</odoo>
